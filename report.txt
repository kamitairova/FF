Отчёт за 07.02.2026

Начало работы

1. **Настройка проекта и структуры репозитория**

   * Создала основную структуру проекта: папки `/backend`, добавила файлы `docker-compose.yml`, `README.md`.
   * Инициализировала Git-репозиторий.
   * Подготовила Docker Compose для трёх сервисов: база данных PostgreSQL, API-бэкенд и Nginx (пока без конфигурации веб-сервера).

2. **Запуск базовой инфраструктуры**

   * Через Docker Compose подняла PostgreSQL.
   * Проверила, что база данных успешно запускается и доступна на порту 5000.

3. **Настройка бэкенда (Node.js + Express + TypeScript)**

   * Инициализировала проект в `/backend` и создала `package.json` с необходимыми зависимостями: Express, TypeScript, Prisma, bcrypt, jsonwebtoken, multer, nodemailer, Zod, Jest.
   * Настроила `tsconfig.json` для корректной работы TypeScript.
   * Реализовала базовый Express-сервер с middleware для логирования в формате JSON, добавила эндпоинт `/api/health` и глобальный обработчик ошибок в формате, указанном в техническом задании.
